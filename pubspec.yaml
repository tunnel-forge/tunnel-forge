name: tunnel_forge
description: Tunnel Forge - L2TP/IPsec VPN for Android.
publish_to: "none"

version: 0.2.0+9

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  archive: ^4.0.7
  cross_file: ^0.3.4+2
  cupertino_icons: ^1.0.8
  file_picker: ^10.3.2
  flutter_secure_storage: ^10.0.0
  share_plus: ^11.1.0
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.7

flutter:
  uses-material-design: true
